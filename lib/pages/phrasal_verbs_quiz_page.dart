import 'package:flutter/material.dart';

class PhrasalVerbsQuizPage extends StatelessWidget {
  const PhrasalVerbsQuizPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Theme.of(context).colorScheme.background,
      body: const Center(
        child: Text("Phrasal Verbs Quiz Page"),
      ),
    );
  }
}
