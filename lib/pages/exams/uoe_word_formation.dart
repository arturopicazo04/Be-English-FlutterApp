import 'package:flutter/material.dart';

class WordFormation extends StatelessWidget {
  const WordFormation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
